using System;

class math {
	static void Main(){
		double sqrt2 = Math.Sqrt(2.0);
		Console.Write($"sqrt(2)={sqrt2}\n");
		Console.Write($"sqrt2*sqrt2={sqrt2*sqrt2} (should be equal to 2)\n");
		
		double expPi = Math.Exp(PI)
		Consol.Write(
	}	
}
